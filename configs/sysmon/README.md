# Sysmon Configuration

This folder contains the Sysmon configuration used in this project.

The configuration file was used while installing Sysmon to enable detailed endpoint monitoring.

Installation command:

```cmd
Sysmon64.exe -accepteula -i sysmonconfig-export.xml
```
